services:
  codex-proxy-rs:
    build:
      context: .
      network: host
    ports:
      - "18080:18080"
    volumes:
      - ./config.yaml:/app/config.yaml:ro
      - ./auths:/app/auths
      - ./data:/app/data
